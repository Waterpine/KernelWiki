---
id: pr-sglang-4165
repo: sgl-project/sglang
pr: 4165
title: DeepGemm integrate to sgl-kernel
author: laixinn
date: 2025-03-07
url: https://github.com/sgl-project/sglang/pull/4165
source_category: upstream-code
architectures:
- sm90
tags:
- gemm
- jit-compilation
techniques:
- jit-compilation
- fine-grained-quantization
hardware_features:
- fp8
- wgmma
- tma
kernel_types:
- gemm
- grouped-gemm
languages:
- cuda-cpp
- python
captured_at: 2026-04-16
status: merged
merge_sha: c553e160
inclusion_reason: manually curated, Blackwell kernel relevance
changed_paths:
- .gitmodules
- sgl-kernel/3rdparty/deepgemm
- sgl-kernel/build.sh
- sgl-kernel/pyproject.toml
- sgl-kernel/setup.py
- sgl-kernel/tests/test_deep_gemm.py
---

## Summary

Integrates DeepGEMM and its JIT-compiled FP8 kernels into the `sgl-kernel` build and package.

## Problem

SGLang lacked integration with DeepGemm's high-performance GEMM kernels. The build system needed to be extended to incorporate DeepGemm as a third-party library alongside the existing AOT-compiled gemm components, enabling JIT-compiled FP8 matrix multiplications optimized for specific tensor dimensions and hardware.

## Solution / Techniques

- Modified `setup.py` to package DeepGEMM alongside the existing `sgl-kernel` components
- Symlinks CUTLASS and CuTe header files required for JIT compilation at runtime
- Copies DeepGemm files into the Python package during wheel creation
- JIT compilation strategy defers kernel compilation until runtime, allowing dynamic gemm based on tensor dimensions and hardware characteristics
- Requires setuptools >= 75.0 for compatibility

## Key Code

```python
# DeepGemm JIT compilation - kernels are compiled at runtime
# targeting specific tensor dimensions and hardware capabilities
import deep_gemm
# Uses CUTLASS headers for JIT: cute.h, tcgen05.h
```

## Performance

Benchmarks on Hopper/Blackwell GPUs:

| Matrix Size (m) | TFLOPS |
|-----------------|--------|
| 64 | 193-423 |
| 128 | 347-789 |
| 4096 | 595-1392 |

Grouped operations achieve 1.3+ TFLOPS at production scale, supporting tensor parallelism patterns common in large language model inference.
