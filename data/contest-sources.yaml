## Public contest-submission source mappings consumed by
## scripts/collect_contest_code.py.
##
## A mapping belongs here only when an accessible organizer record establishes
## the participant/rank and the mapped code is the actual submission (or an
## explicitly labeled author-published copy). Participant worklogs and teaching
## extracts are not silently promoted to ranked submissions.

gpu-mode-nvfp4:
  # No accessible organizer record currently establishes exact final ranks or
  # provides verbatim public submissions for these four problems.
  problem-1-gemv: {}
  problem-2-gemm: {}
  problem-3-gated-dual-gemm: {}
  problem-4-grouped-gemm: {}

flashinfer-mlsys26:
  # Final winner names are recorded on the contest source pages. No winner-code
  # bundle is mapped here until the exact public submission is established.
  track-a-fused-moe: {}
  track-b-sparse-attention: {}
  track-c-gated-delta-net: {}
